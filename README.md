# HPL 4.0 — Vercel Version

A standalone React/Vite version of the HPL 4.0 website.

## Run locally
npm install
npm run dev

## Build
npm run build

## Vercel
Import this folder/repository into Vercel. Framework preset: Vite.
Build command: npm run build
Output directory: dist

The registration button uses the supplied Google Form URL.
